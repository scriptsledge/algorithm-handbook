---
title: 3423. Maximum Difference Between Adjacent Elements in a Circular Array
tags:
  - Array
  - Easy
---

# 3423. Maximum Difference Between Adjacent Elements in a Circular Array

<div class="grid cards" markdown>

-   :material-youtube: **Watch the Solution**
    [Watch Video](https://youtube.com/shorts/OHz8XKLe1_E){ .md-button }

-   :material-github: **View on GitHub**
    [Source Code](https://github.com/scriptsledge/algorithm-handbook/tree/main/src/3423){ .md-button }

-   :simple-leetcode: **LeetCode Solution**
    [Read Post](https://leetcode.com/problems/maximum-difference-between-adjacent-elements-in-a-circular-array/solutions/7543373/on-solution-simple-iteration-circular-ar-12ud){ .md-button }

</div>

## 📝 Problem Description

!!! info "Problem Statement"
    Given a circular array `nums`, find the maximum absolute difference between adjacent elements.

    Note: In a circular array, the first and last elements are adjacent.

### Examples

**Example 1:**
```text
Input: nums = [1,2,4]
Output: 3
Explanation: Because nums is circular, nums[0] and nums[2] are adjacent. They have the maximum absolute difference of |4 - 1| = 3.
```

**Example 2:**
```text
Input: nums = [-5,-10,-5]
Output: 5
Explanation: The adjacent elements nums[0] and nums[1] have the maximum absolute difference of |-5 - (-10)| = 5.
```

### Constraints
*   `2 <= nums.length <= 100`
*   `-100 <= nums[i] <= 100`

## 🧠 Intuition

The problem asks for the maximum absolute difference between adjacent elements in a circular array. In a standard array, we would just check pairs $(i, i+1)$. In a circular array, we must also consider the pair consisting of the last and first elements.

## 💡 Approach

1.  Initialize `maxDistance` to 0.
2.  Iterate through the array from `i = 0` to `n - 1`.
3.  For each element at index `i`, its "next" neighbor in a circular array is at index `(i + 1) % n`.
4.  Calculate the absolute difference: `abs(nums[i] - nums[(i + 1) % n])`.
5.  Update `maxDistance` if the current difference is larger.
6.  Return `maxDistance`.

## 💻 Code

=== "C++"

    ```cpp
    --8<-- "src/3423/solution.cpp"
    ```

## 📊 Complexity Analysis

*   **Time Complexity:** $O(N)$
    *   We traverse the array exactly once.

*   **Space Complexity:** $O(1)$
    *   We only use a constant amount of extra space for variables.